generates:
  ./sdk.ts:
    schema:
      - "./pages/api/schema.gql"
      - "./blog/schema.gql"
    documents:
      - "./blog/queries.gql"
    plugins:
      - typescript
      - typescript-operations
      - typescript-resolvers
      - typescript-react-apollo
    config:
      enumsAsTypes: true
